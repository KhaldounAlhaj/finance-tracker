# Finance v10.3 QA Corrections Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans and implement test-first.

**Goal:** Correct reconciliation accounting, unify summaries and drill-through classification, expose v10.2 monthly figures in closed months, and synchronize filter behavior.

**Architecture:** Add a pure `classifyMoneyEntry(state, entry)` function in the financial core and consume it from `monthlyMoneySummary`, `monthMetrics` behavior, and Log drill-through. Add a compact historical money renderer using the same summary outputs rather than duplicating formulas.

**Tech Stack:** Dependency-free HTML/CSS/JavaScript PWA and Node built-in tests.

## Global Constraints

- Preserve modelVersion 9 and `khaldoun_finance_v3`.
- Never rewrite stored financial data.
- No external dependencies, telemetry or network calls.
- Bump the service-worker cache to `finance-v10.3`.

### Task 1: Correct accounting classification

- [ ] Add failing finance tests proving balance adjustments have zero money deltas and card-funded legacy goals do not reduce liquidity.
- [ ] Add `classifyMoneyEntry(state, entry)` and route `monthlyMoneySummary` through it.
- [ ] Add the explicit zero branch to `entryDeltas`.
- [ ] Run finance tests to GREEN.

### Task 2: Reconcile drill-throughs and filters

- [ ] Add failing design contracts for shared classification, drill reset and filtered empty-state copy.
- [ ] Use the shared classifier for spending and goal drills.
- [ ] Reset contradictory controls on drill open and show a matching-results empty state.
- [ ] Render negative source totals with a minus sign.
- [ ] Run finance/design/import tests to GREEN.

### Task 3: Closed-month money view

- [ ] Add a failing design contract for historical liquidity, source, debt and goal surfaces.
- [ ] Add compact closed-month panels and render them from `monthMetrics` plus `monthlyMoneySummary` for the selected month.
- [ ] Update active drill month when month navigation changes.
- [ ] Verify historical source drill-through.

### Task 4: Release

- [ ] Change release test to require `finance-v10.3` and verify RED.
- [ ] Bump cache, update documentation/changelog and regenerate docs.
- [ ] Run the complete test suite, privacy/dependency scans, diff check and phone/theme browser QA.
- [ ] Record the rewritten-history and v10.3 work in COLLAB-LOG.md.
- [ ] Commit and push under the owner’s authorization.
